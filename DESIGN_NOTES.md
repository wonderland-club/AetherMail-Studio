## 📋 新的简化配置示例

### 配置结构
```json
{
  "templates": {
    "advantages": {
      "name": "优点清单",
      "description": "发送一份详细的优点清单邮件",
      "file": "给你的200个优点.md",
      "required_fields": ["recipient_email"],
      "email_subjects": "分享美好时光,想念你,今天的心情,特别的一天,问候与关怀,节日祝福,生日快乐,感谢信"
    }
  }
}
```

### 设计优势
1. **简化配置**: 将复杂的主题分类结构简化为简单的逗号分隔字符串
2. **模板关联**: 每个模板都有自己独立的主题列表，更加灵活
3. **易于维护**: 配置文件更加简洁，易于阅读和修改
4. **向后兼容**: API接口保持不变，用户无需修改调用代码

### 使用示例
```bash
# 使用随机主题
curl -X POST "http://127.0.0.1:5000/api/send" \
  -H "Content-Type: application/json" \
  -d '{
    "recipient_email": "test@example.com",
    "template": "advantages"
  }'

# 使用自定义主题
curl -X POST "http://127.0.0.1:5000/api/send" \
  -H "Content-Type: application/json" \
  -d '{
    "recipient_email": "test@example.com",
    "template": "advantages",
    "custom_subject": "🎉 特别的祝福！"
  }'
```
